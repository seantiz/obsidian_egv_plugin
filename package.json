{
	"name": "export-graph-view",
	"version": "1.2.2",
	"description": "Export your Obsidian vault's graph view to mermaid and dot format.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [],
	"author": "Sean McGhee",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^24.0.10",
		"esbuild": "^0.25.5",
		"obsidian": "^1.8.7",
		"tslib": "^2.8.1",
		"typescript": "^5.8.3"
	}
}
